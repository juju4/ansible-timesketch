---

- hosts: all
  vars:
    - elasticsearch_script_inline: true
    - postgresql_users:
        - name: timesketch
          password: timesketch
          db: timesketchdb
    - postgresql_databases:
        - name: timesketchdb
    - neo4j_password: timesketch_neo4j_change_me
    - timesketch_process: true
  roles:
    - juju4.timesketch
